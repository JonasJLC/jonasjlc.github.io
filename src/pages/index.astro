---
import Contact from '../components/Contact.astro';
import Hero from '../components/Hero.astro';
import Notes from '../components/Notes.astro';
import ProjectList from '../components/ProjectList.astro';
import { site } from '../content/site';
import BaseLayout from '../layouts/BaseLayout.astro';

const description = `${site.name}: a compact project index for software, automation, and AI experiments.`;
---

<BaseLayout title={`${site.name} | Portfolio`} description={description}>
  <Hero />
  <main>
    <ProjectList projects={site.projects} />
    <Notes />
    <Contact />
  </main>
  <footer>
    <a href="#top">{site.name}</a>
    <span>{new Date().getFullYear()}</span>
  </footer>
</BaseLayout>
